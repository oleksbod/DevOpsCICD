applications:
    - name: django-app
      namespace: default
      project: default
      source:
          repoURL: "https://github.com/oleksbod/DevOpsCICD.git"
          path: "lesson-7/charts/django-app"
          targetRevision: main
          helm:
              valueFiles:
                  - values.yaml
      destination:
          server: "https://kubernetes.default.svc"
          namespace: default
      syncPolicy:
          automated:
              prune: true
              selfHeal: true
          syncOptions:
              - CreateNamespace=true

repositories:
    - name: infra-repo
      url: "https://github.com/oleksbod/DevOpsCICD.git"
      type: git

repoConfig:
    insecure: "true"
    enableLfs: "true"
